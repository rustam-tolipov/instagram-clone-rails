<%= form_with(model: Comment.new, url: post_comments_path(post)) do |f| %>
  <%= f.text_field :content, class: "form-control border-0", placeholder: "Add a comment" %>
  <%= f.submit "Comment", class: "btn btn-primary" %>
<% end %>